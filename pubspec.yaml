name: log_collector
description: Provides log filtering, buffering, and retry.
author: Ryo Miyake <ryo@miyake.id>
homepage: https://github.com/miyakeryo/log_collector
version: 1.0.4

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  path_provider: ^2.0.2
  synchronized: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
